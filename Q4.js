// Create an array of 4 colors. Use join('-') to convert the array into a single string and
// print it.

const colors =["red","blue","green","yellow"];
const result = colors.join("_");
console.log(result)